
come è fatta la pipeline di VVF_AFM_MS_Backend_IAS


primo atto che fa pipeline è:
- allestire l'ambiente su cui fare la build. rende indipendente il sistema rispetto a un ambiente dove fare l'istallazione e le build. 

bisogna dare la clausula vmImageName che gli dice che immagine scaricare. es ubuntu, windows... è unica (?) .. posso fare anche piu variabili e utilizzarle nei job in modo doppio o multiplo.

resources:
     -repo self 
gli dice parti da questo repository e scarica qui... 

trigger:
-branch

poi c'è il `#versioning


stages:  (parola chiave per indicare i passaggi, tra questi vi sono gli step, job, task, script)
nei primi passi c'è il job di build (fa quelle cose della vm)



-pwsh: comando powershell
con echo gli dici cosa scrivere in console
-task: UseDotNet@2 
(glu dice di installare dotnet con quella versione)
-script: dotnet nuget pacchetto
(gli dice esattamente il comando da terminale)

-task di build ....

buildconfiguration 'Debug' oppure 'Release'


poi ci sono alcune variabili sue interrne, che sa lui come valorizzare, non possiamo saperlo perche lo fa sulla macchina virtuale che ha installato lui poco prima.


sempre nei task (task fanno riferimento a delle funzioni predefinite delle pipeline)
poi c'è la copia dei file e la pubblicazione degli artefatti nella pipeline..













notebookLM..